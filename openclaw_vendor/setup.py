from setuptools import find_packages, setup


setup(
    name="openclaw",
    version="0.1.0",
    description="Local lightweight OpenClaw-compatible tool orchestration shim",
    packages=find_packages(),
    python_requires=">=3.9",
)
